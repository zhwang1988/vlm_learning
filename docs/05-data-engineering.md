# 05 · 图文数据工程

> 对应 Day 7–Day 12

## 概念

### 一句话

> **后训练的效果上限由数据决定，不是由超参数决定。**

这不是鸡汤。在同样的 3B 基座、同样的 LoRA 配置下，一个精心构造的 10k 数据集和一个随便爬的 100k 数据集，前者通常赢。原因是后训练的目标不是「学新知识」，而是**把模型的行为对齐到你的业务分布上**——这件事完全靠数据刻画。

### 客服图文数据的 Taxonomy

先分类，再采数据。不做分类的后果是：你采了 5000 条「问尺码」，以为数据够了，结果上线发现 30% 的真实咨询是「收到的和图片不一样」。

本项目用**二维矩阵**：意图 × 图像类型。

**8 大意图**：

| 意图 | 说明 | 典型问法 |
|---|---|---|
| `size_fit` | 尺码合身 | 「我 170/60 穿 M 可以吗」 |
| `quality_issue` | 质量/瑕疵 | 「这里线头是不是有问题」 |
| `color_mismatch` | 色差 | 「实物是米白还是米黄」 |
| `material` | 材质成分 | 「这个是纯棉还是混纺」 |
| `styling` | 搭配建议 | 「这件配什么裤子好看」 |
| `logistics` | 物流 | 「我的快递到哪了」 |
| `return_refund` | 退换货 | 「这个能退吗，怎么退」 |
| `pre_sale_general` | 售前其他 | 「有别的颜色吗」「什么时候补货」 |

**6 类图像类型**：

| 图像类型 | 特征 | 对模型的挑战 |
|---|---|---|
| `product_main` | 白底商品主图 | 简单，模型已很强 |
| `model_wearing` | 模特上身图 | 需要推断体型、版型 |
| `detail_closeup` | 面料/做工特写 | 需要细粒度纹理理解 |
| `defect_photo` | 用户拍的瑕疵照 | **最难**：光线差、构图乱、需要判断是否正常 |
| `size_chart` | 尺码表（表格图） | OCR + 表格结构理解 |
| `user_compare` | 收到的实物 vs 商品图对比 | 需要多图对比推理 |
| `logistics_receipt` | 快递单/物流截图 | OCR + 信息抽取 |

**矩阵填写**：不是每个格子都需要（比如 `logistics × model_wearing` 没意义）。填完你大概会得到 30 个左右的有效格子，**每个格子至少 200 条**就是 6000 条——这就是你的目标规模。

**Day 8 的产出就是这个矩阵 + 每个格子的真实例子**。去淘宝/京东/Amazon 的真实问答区逛一圈，把真实问题抄下来比凭空想强十倍。

### 数据来源的三种范式

```
范式 A · 真实数据（最好，最难拿）
  来源：商家后台的历史客服记录、公开的电商问答、自己扮演买卖双方
  优点：分布真实，覆盖「脏」的情况
  缺点：量少、有隐私问题、标注成本高

范式 B · 合成数据（主力，本项目主线）
  用强模型（GPT-4o / Qwen-VL-Max / Claude）按 taxonomy 批量生成
  优点：可控、便宜、能定向补短板
  缺点：同质化严重、有模型偏见、可能事实错误

范式 C · 改写真实数据（性价比最高）
  拿真实的商品图和真实用户问题，用强模型生成标准答案
  优点：分布真实 + 标注质量高
  缺点：需要先有真实问题集
```

**正确做法是 B + C 混合**，而不是纯 B。纯合成数据训出来的模型会有一种很明显的「AI 味」——答案结构过于工整、爱用「首先其次最后」、遇到不确定的东西也硬答。

### 合成数据的四个陷阱

1. **分布坍缩**：让模型生成 1000 条「尺码咨询」，你会发现 800 条长得很像。解法：**生成前先采样多样性种子**（不同商品类目、不同用户口吻、不同情绪）。
2. **答案过于完美**：真实客服的回答会带口语、会有不确定、会主动问澄清。解法：**在 prompt 里明确要求口语化、允许说不知道**。
3. **事实幻觉**：模型会编造商品参数。解法：**生成时提供结构化上下文**（类目、尺码、材质、价格），要求答案只能基于上下文。
4. **拒答模式缺失**：合成数据里几乎没有「这个问题我无法回答」的样本，导致模型上线后什么都敢答。解法：**主动合成 5–10% 的拒答/转人工样本**。

第 4 条最容易被忽略，但在客服场景是刚需——**答错比不答的代价高得多**。

### 数据清洗的五个环节

```
原始数据
  ↓ ①格式校验      —— JSON 可解析、字段完整、图片能打开
  ↓ ②规则过滤      —— 长度区间、无敏感词、无联系方式、语言为中文
  ↓ ③图文一致性     —— 图和问题真的相关（用 CLIP 相似度卡阈值）
  ↓ ④去重          —— 图片 pHash 去重 + 文本语义去重
  ↓ ⑤质量打分      —— 用强模型给每条样本打 1–5 分，卡 4 分
干净数据
```

②③④⑤ 每一步都要**出统计报告**（输入多少、淘汰多少、为什么）。没有报告你无法判断数据质量问题出在哪。

### 去重：图片和文本要用不同方法

**图片：pHash（感知哈希）**

```python
# 把图缩到 8×8 灰度 → 算 DCT → 取左上 8×8 → 大于中位数的记 1 → 64 bit
# 两张图的 pHash 汉明距离 < 5 视为重复
```

能抓住「同一张图不同压缩质量」「裁掉一点边」这类重复。

**文本：embedding 语义去重**

```python
# 用 sentence-transformers 编码，余弦相似度 > 0.92 视为重复
```

**为什么不能只用一种**：同一张图配不同问题（不是重复，是有效样本）；同一个问题配不同图（也是有效样本）。**要去重的是「图 + 文」这个整体**。所以最终用的是组合键：

```python
key = (pHash(image), embedding_cluster(text))
```

还要注意：**评测集绝对不能和训练集有重叠**。这包括图片重叠——如果评测集的图在校验集里出现过，分数会虚高。这就是所谓 **image-level leakage**，是 VLM 评测最常见的作弊来源。Day 20 构造评测集时必须做 pHash 交叉检查。

### 数据配比

不是所有数据都一样重要。推荐配比（总量 10k）：

| 类别 | 占比 | 说明 |
|---|---|---|
| 领域核心（你的 8 大意图） | 60% | 主力 |
| 拒答/转人工 | 8% | 安全底线，少了会出事 |
| 通用视觉能力（保持不遗忘） | 20% | 用开源通用 VQA 数据混入 |
| 纯文本能力（保持语言能力） | 7% | 纯文本指令数据，防止语言能力退化 |
| 难例（bad case 反哺） | 5% | 第 4 周之后的错误分析产物 |

**最后两行很关键**：只训领域数据，模型会**灾难性遗忘**——通用能力退化，甚至语言表达变差。加入通用数据混合训练是标准做法，叫 **replay / experience replay**。

### 对话模板与 Label Mask

训练时最重要的是**只对 assistant 的回复算 loss**。

```
<|im_start|>system
你是一个专业的电商客服助手...<|im_end|>
<|im_start|>user
<|vision_start|><|image_pad|><|vision_end|>这件衣服是纯棉的吗<|im_end|>
<|im_start|>assistant
从吊牌标注看是 95% 棉 + 5% 氨纶...<|im_end|>

label:  system 部分 → -100
        user 部分   → -100      ← 关键：不要学怎么提问
        assistant 部分 → 保留 token id
        <|im_end|>   → 保留（要学停止）
```

**常见错误**：

- 忘记 mask `system` → 模型学会复述 system prompt
- mask 掉了 assistant 的 `<|im_end|>` → 模型不知道何时停，生成刹不住
- 多轮对话只保留最后一轮 → 训练信号变少；正确做法是全部保留，每轮 assistant 都算 loss
- 用 base 模型的 template → 少了 `<|im_start|>` 等特殊标记，模型输出混乱

**多图样本**：`<|vision_start|><|image_pad|><|vision_end|>` 出现几次，就有几张图，`images` 列表顺序必须与之一致。

## 工程细节

### 合成数据 prompt 模板（Day 9 用）

```python
SYNTH_SYSTEM = """你是一个电商客服数据生成器。给定商品信息和图像类型，生成一条真实的顾客咨询和客服回复。

要求：
1. 顾客提问必须口语化，可以带情绪（着急、不满、犹豫），可以打字错误
2. 客服回复必须：先回应用户情绪，再基于【已知商品信息】回答，不确定就明确说需要核实
3. 严禁编造商品信息中不存在的事实
4. 回复控制在 80–200 字，不要用「首先/其次/最后」的模板结构
5. 输出 JSON：{"user_query": "...", "assistant": "...", "intent": "...", "difficulty": 1-5}
"""
```

**关键技巧**：把「不要用首先其次最后」这种**负面约束**写进去。不写的话你会得到一堆 AI 味极重的答案，训出来的模型一开口就像客服机器人模板。

### 从「图 + 意图」造样本的完整流程

```python
for product in product_pool:             # 商品池（几百个）
    for image in product.images:          # 每商品 5–10 张图
        for intent in sample_intents(3):  # 每张图随机抽 3 个意图
            seed = {
                "category": product.category,
                "attrs": product.attributes,      # 材质/尺码/颜色/价格
                "image_type": classify_image(image),
                "user_persona": random.choice(PERSONAS),  # 急躁/理性/新手...
                "emotion": random.choice(EMOTIONS),
            }
            sample = call_strong_model(SYNTH_SYSTEM, seed, image)
            yield validate(sample)
```

`PERSONAS` 和 `EMOTIONS` 是抗同质化的关键。没有它们，1000 条样本会长得一模一样。

### 图片分桶（Bucketing）

因为 Qwen2.5-VL 是动态分辨率，同一个 batch 里图片 token 数不同。如果随机组 batch，padding 会浪费大量显存。

```python
# 按 visual token 数分桶，每桶内 token 数相近
buckets = defaultdict(list)
for s in dataset:
    n = estimate_visual_tokens(s.image)
    bucket_id = min(int(n // 256), 4)     # 0-4 共 5 桶
    buckets[bucket_id].append(s)

# 组 batch 时从同一个桶里取
```

**收益**：实测能省 20–40% 显存。**这是 Day 11 的一个可选优化**，如果时间紧可以跳过，但值得知道。

### 数据集卡片

Day 12 要产出 `DATASET_CARD.md`，必须包含：

```markdown
# Dataset Card: CX-VLM-SFT-v0

## 规模
- 训练 8,000 / 验证 1,000 / 测试 1,000

## 分布
- 意图分布表（8 类）
- 图像类型分布表（6 类）
- 难度分布（1–5）
- 平均轮数、平均 token 数

## 来源
- 合成模型：xxx（版本、日期）
- 种子商品池：xxx
- 真实数据占比：x%

## 已知缺陷
- 某类目覆盖不足
- 缺陷照（defect_photo）样本偏少
- 某类问题答案偏模板化

## 许可与隐私
- 含真实用户数据：是/否
- 是否脱敏：...
```

**「已知缺陷」这一节最有价值**。写不出来说明你没有认真看过自己的数据。

## 自检问题

1. 为什么说后训练的效果上限由数据决定？
2. 你的客服数据矩阵里，最难的两个格子是什么？为什么难？
3. 纯合成数据训出来的模型有什么典型症状？怎么避免？
4. 为什么必须加入 5–10% 的拒答样本？
5. 图片去重为什么用 pHash 而不是 embedding？文本为什么用 embedding 而不是 pHash？
6. 什么是 image-level leakage？它怎么让评测分数虚高？
7. Label mask 里哪几个部分必须置 -100？漏掉 assistant 的 `<|im_end|>` 会怎样？
8. 只训领域数据会发生什么？怎么防？

---

**上一篇**：[04-qwen25vl.md](04-qwen25vl.md) · **下一篇**：[06-sft-training.md](06-sft-training.md)
