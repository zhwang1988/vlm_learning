# Day 28 · 可验证奖励与 GRPO（进阶，可跳过）

> 预计 3–4h ｜ 📓 `notebooks/day-28_verifiable_rewards.ipynb` ｜ 💻 本地 · `src/train/rewards.py`
> 前置：Day 27

## 今日目标（一句话）

实现三个**可验证**奖励（JSON 格式可解析 / 工具调用正确 / 引用图片区域正确），跑通单元测试；并回答「客服场景下为什么可验证奖励比人偏好更划算」。

## 一、读（60 min）

材料：
- `docs/07-alignment.md` 第 5–6 节（可验证奖励 / GRPO / RLVR）
- `src/train/rewards.py` 的 `RewardWeights` —— 权重怎么配是今天的核心问题

思考题（先自己想，答案在讲义或代码注释里）：
1. 「格式合规」可以被程序验证，「语气友好」不能 —— 这两类奖励该怎么配权重？
2. 为什么说可验证奖励是**免费的数据标注**？
3. 奖励函数写错了会怎样？（比数据错更可怕，因为模型会全力钻空子）

## 二、写（100 min）

`src/train/rewards.py`（已给实现）

| 函数 / 文件 | 你要做什么 |
|---|---|
| `reward_format()` | JSON 能否解析 + 字段齐全 + 值域合法 |
| `reward_tool_call()` | 工具名是否在白名单 + 参数是否齐全 + **是否重复调用** |
| `reward_slot_filling()` | 订单号 / 尺码 / 颜色 等槽位是否被正确提取 |
| `reward_grounding()` | 回答里的视觉描述是否与图对上（引用区域） |
| `reward_hallucination_penalty()` | 出现图里没有的物体 → 扣分 |
| `reward_refusal()` | 该拒答时拒答（而非瞎编） |

> 奖励函数是**代码**，不是数据。它的 bug 会被模型以最激进的方式利用 —— 
> 所以每一个奖励都要有单元测试，且要专门写「对抗样本」测试它。

## 三、跑（本地（无需 GPU））

```bash
# 跑全部奖励函数的单元测试（本地，无 torch）
python -m src.train.rewards
```

期望输出（节选）：
```
奖励函数单元测试
==============================================================================
[格式] 合法 JSON            → 1.000   ✓
[格式] 缺字段               → 0.300   ✓
[格式] 不是 JSON            → 0.000   ✓
[工具] 正确调用查订单        → 1.000   ✓
[工具] 编造工具名 get_weather → 0.000   ✓   ← 白名单拦住了
[工具] 漏参数               → 0.400   ✓
[槽位] 订单号提取正确        → 1.000   ✓
[定位] 描述的物体图里有       → 1.000   ✓
[定位] 描述的物体图里没有     → 0.000   ✓   ← 幻觉惩罚
[拒答] 该拒答时拒答          → 1.000   ✓
==============================================================================
✓ 全部通过（含 3 个对抗样本）
```

## 四、验收清单

- [ ] 奖励函数单元测试全部通过，**且包含对抗样本测试**
- [ ] 能说清「客服场景下为什么可验证奖励比人偏好更划算」（三个理由）
- [ ] 能说出奖励配比不当会怎样（比如格式奖励权重过高 → 只输出格式不解决问题）
- [ ] 知道 GRPO 和 DPO 的最根本差别（要不要 reward model / 要不要配对数据）

## 五、容易踩的坑

1. **格式奖励权重过高** —— 模型学会输出完美 JSON 但内容空洞。这是最经典的 reward hacking。
2. 工具调用奖励不校验参数 —— 模型学会「调用工具但不填参数」，形式上通过。
3. 幻觉惩罚误伤诚实的「不确定」 —— 模型学会硬编一个答案，反而不说不知道了。
4. 奖励函数没有对抗测试 —— 你自己想不出钻空子的方式，模型能。
5. 跳过这一天 —— **这是允许的**（止损线第 1 条）。但「可验证奖励」的思想必须理解，它是 Day 31–36 工具调用的理论基础。

## 六、打卡

复制 `progress/daily-log.md` 模板到文件末尾，三行起：读了什么 / 写了什么 / 卡在哪。

> **可跳过**：如果进度落后，直接看讲义 + 跑一遍单元测试就够了，不必深挖 GRPO。
