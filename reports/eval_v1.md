# 评测报告 · fake_good vs fake_bad vs fake_silent

> 生成自 3 个 run：fake_good, fake_bad, fake_silent
> 数据来源：`src/eval/run_eval.py` 写出的 `eval_*_raw.jsonl`

## 总览

| 指标 | fake_good | fake_bad | fake_silent |
|---|---|---|---|
| 样本数 | 273 | 273 | 273 |
| 规则通过率 | 100.0% | 17.6% | 11.0% |
| 规则均分 | 0.996 | 0.725 | 0.648 |
| judge 均分 | — | — | — |
| judge 已打分条数 | 0 | 0 | 0 |
| P95 延迟 | 1736 ms | 1711 ms | 1736 ms |

> 「规则通过率」是 `rule.passed` 为真的样本占比；「规则均分」是 `rule.score` 的均值。二者含义不同：一条回答可以拿了很高的单项分但最终 `passed=False`。
>
> 「—」= 该项**没有可用的分母**，不是 0 分。受影响的行：judge 均分。最常见的原因是本次评测没跑 LLM judge（没配 API key / 用 `scripts/fake_eval.py` 造的预测），所以 judge 列整列为空 —— 这不代表模型 judge 得分低。

## 分层结果

### 按难度分层

> 「规则通过率」= 该层里 `rule.passed` 为真的比例；「规则均分」= 该层 `rule.score` 的均值。两个数含义不同，别混读。

| 难度 | fake_good 通过率 | fake_bad 通过率 | fake_silent 通过率 | fake_good 均分 | fake_bad 均分 | fake_silent 均分 |
|---|---|---|---|---|---|---|
| L1 | 100.0% | 16.0% | 0.0% | 1.000 | 0.713 | 0.600 |
| L2 | 100.0% | 2.9% | 0.0% | 1.000 | 0.727 | 0.658 |
| L3 | 100.0% | 33.8% | 0.0% | 1.000 | 0.760 | 0.600 |
| L4 | 100.0% | 13.3% | 100.0% | 0.968 | 0.663 | 0.904 |

→ 各层通过率都很高且差距很小（极差 0.0%，最低 100.0%），**没有明显弱层**。下一步应该去提高评测难度，而不是补数据。

### 各意图通过率（基于 `fake_good`，**无最弱项**）

| 意图 | 通过率 | 样本数 |
|---|---|---|
| quality_issue | 100.0% | 55 |
| material | 100.0% | 43 |
| logistics | 100.0% | 23 |
| styling | 100.0% | 18 |
| size_fit | 100.0% | 44 |

→ 全部 8 个意图通过率都在 100.0% 以上（极差 0.0%），**分不出最弱项**。当前评测对这个模型偏简单，该加难度，而不是补某一类的数据。

## 失败分析（基于 `fake_silent`）

### 失败模式

共 243 / 273 条失败（89.0%）

| 失败类型 | 数量 | 占失败样本 | 典型样本 |
|---|---|---|---|
| 要素缺失 | 223 | 92% | 这件是什么颜色？ |
| 质量不达标 | 20 | 8% | 从图片看这个面料会不会起球？ |

### 下一步建议

1. **各层都没有明显短板**（最低 L1 100.0%，极差 0.0%）。当前评测对这个模型**偏简单**，应该先加难度（L3 多图对比 / L4 边界场景），而不是继续补同分布的数据。
2. **针对「要素缺失」做专项**：占失败样本 92%（223 条），是最大的一类。
   → 具体做法：检查这类问题的 must_contain 是否过严；若合理，则补该要素的训练样本
3. ⚠️ **从 fake_good 到 fake_silent 退化了 -89.0%** —— 这是一次性能回归，先别继续改，回去核对：数据有没有变、评测集有没有改、超参是不是动了。

## 复现信息

| 项 | 值 |
|---|---|
| run `fake_good` | 273 条（失败 0，其中调用出错 0） |
| run `fake_bad` | 273 条（失败 225，其中调用出错 0） |
| run `fake_silent` | 273 条（失败 243，其中调用出错 0） |

> 「失败」口径与上方失败分析一致：推理出错 或 规则判定不通过 或 judge 低于阈值。

> 报告由 `src/eval/report.py` 生成；改数字请重跑 `run_eval`，不要手改本文件。
